# Application
spring.application.name=auth-service
server.port=8097

# Database Manage
spring.datasource.url=jdbc:mysql://localhost:3306/food_delivery_db?useSSL=false&serverTimezone=UTC

spring.datasource.username=root
spring.datasource.password=Indiadaily@2025
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA - Changed to 'update' for auto table creation
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration - Updated secret key
jwt.secret=foodDeliverySecretKeyForJWTToken2026VerySecureAndLongKey

jwt.access-token-expiration=900000
jwt.refresh-token-expiration=604800000

# OTP Configuration
otp.expiration-minutes=5
otp.max-attempts=3

# Account Security
account.max-failed-login-attempts=5
account.lock-duration-minutes=30

# CORS - Updated for production
cors.allowed-origins=http://localhost:3000,http://localhost:4200,http://localhost:5173

# Logging (Optional - for debugging)
logging.level.com.fooddelivery.authservice=DEBUG
logging.level.org.springframework.security=DEBUG
